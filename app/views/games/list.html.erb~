<h1 class="setsolver">Setsolver: List of all games</h1>
<p align=right>
<%= link_to '[Create New Game]', :action => 'create' %>
<%= link_to '[ << Previous Page ]', { :page => @game_pages.current.previous } if @game_pages.current.previous %>
<%= link_to '[ Next page >> ]', { :page => @game_pages.current.next } if @game_pages.current.next %> 
</p>
<div class="setsolver" align=center width=90%>
<table class="setsolver" border=0 width=450>
<% for game in @games %>
  <div class="setsolver-item">
    <p><b>Game #<%= game.id %>:</b> <%= game.player_names %></p>
    <ul>
    <% for player in game.players %>
      <li> <%=player.name %>: <%= link_to "Play as #{player.user.fname}", play_path(game,player) %>
      </li>
    <% end %>
    </ul>
  </div>
  <td><%= link_to 'Delete Game', game_path, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
</div>

<br />

