<h1><%= fb_profile_pic @user %> <%= fb_name @user, :possessive => true, :capitalize => true %> games</h1>
<h2></h2>

<% for game in @player.games %>
    <p><%= game_desc_as_link(game) %> (<%= game.player_names %>):  <%= game_play_link(game) %>
    </p>
<% end %>

<p>
  <%= link_to 'New game', new_game_path %>
</p>

</fb:serverfbml>