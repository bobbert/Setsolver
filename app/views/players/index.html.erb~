<h1>Listing players</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Score</th>
    <th>Is winning?</th>
    <th>Is losing?</th>
    <th />
    <th />
    <th />
  </tr>

<% for player in @players %>
  <tr>
    <td><%=h player.name %></td>
    <td><%=h player.score %></td>
    <td><%=h format_yn(player.winner?) %></td>
    <td><%=h format_yn(player.loser?) %></td>
    <td><%= link_to 'Show', game_player_path %></td>
    <td><%= link_to 'Edit', edit_game_player_path(player.game, player) %></td>
    <td><%= link_to 'Destroy', game_players_path, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New player', new_game_player_path %>
